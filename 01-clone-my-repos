#!/usr/bin/env bash
# cloneRepos

if [[ ${cloneReposFlag:-false} == true ]]; then
  log_info 'Cloning repos.'
else
  log_info 'Cloning repos skipped.'
  return
fi

# Clone repositories using gits
gits clone -r git -v
